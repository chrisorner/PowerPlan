This webapp can be used to simulate the financial benefits of solar energy systems.
The app can be run locally using docker-compose up --build (Docker Desktop must be installed)

It consists of:
1) Load profile generator: http://localhost:8000/profile/
2) Simulation dashboard: http://localhost:8000/simulation/
